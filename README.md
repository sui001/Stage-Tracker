# Stage-Tracker
A single pixel on an led strip follows a performer across a stage. A side-mounted distance sensor tracks their position left-to-right and maps it to an addressable LED strip in real time.
